/******************************************
 * Define a "string-match" base class.
class string_matcher {
public:
// Returns true if string matches, false if not
int match(const char *const string);
...
Define derived classes that match words, numbers, and blank strings.
 * **********************************************/

