#include <iostream>

using namespace std;

int main(){

    int counter(0); //one way of init a var
    int counter2 = 0; //another way
    int product_codes[3] = {10, 972, 45}; // init arrays

    return (0);

}