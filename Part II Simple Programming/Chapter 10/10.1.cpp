
/******************************************
 * 
Create a set of macros to define a type called
RETURN_STATUS and the following values: RETURN_SUCCESS,
RETURN_WARNING, and RETURN_ERROR. Define a macro,
CHECK_RETURN_FATAL,,, that takes a RETURN_STATUS as its
argument and returns true if you have a fatal error.
 * 
 * *************************************/

#include <iostream>

using namespace std;

int main(){
    

    
    return(0);
}

